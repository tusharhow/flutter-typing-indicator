name: flutter_typing_indicator
description: A Flutter package to show typing indicator in chat screen.
version: 0.0.2
homepage: https://github.com/tusharhow/flutter-typing-indicator

environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: